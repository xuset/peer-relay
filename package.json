{
  "name": "wudup",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npm run -s standard && mocha test.js --use-strict",
    "make": "browserify index.js -s Wudup -o bundle.js -i process -i ws -i electron-webrtc",
    "standard": "standard --global it --global describe",
    "browserify": "browserify"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "debug": "^2.2.0",
    "k-bucket": "^3.0.2",
    "simple-peer": "^6.0.4",
    "ws": "^1.1.1"
  },
  "devDependencies": {
    "browserify": "^13.1.0",
    "electron-webrtc": "^0.2.7",
    "mocha": "^2.5.3",
    "standard": "^7.1.2",
    "stats-lite": "^2.0.3"
  }
}
