{
  "name": "wudup",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "standard && mocha test.js --use-strict --timeout 10000",
    "bundle": "browserify -r .:wudup -r bittorrent-dht -d -o bundle.js -i process -i ws -i electron-webrtc",
    "standard": "standard",
    "browserify": "browserify"
  },
  "standard": {
    "globals": [ "it", "describe" ]
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "debug": "^2.2.0",
    "k-bucket": "^3.0.2",
    "simple-peer": "^6.0.4",
    "ws": "^1.1.1"
  },
  "devDependencies": {
    "browserify": "^13.1.0",
    "electron-webrtc": "^0.2.7",
    "mocha": "^2.5.3",
    "standard": "^7.1.2"
  }
}
